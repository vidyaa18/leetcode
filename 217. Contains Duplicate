class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        k = len(set(nums))
        if(k==len(nums)):
            return False
        else:
            return True
