class Solution(object):
    def plusOne(self, digits):
        """
        :type digits: List[int]
        :rtype: List[int]
        """
        m = ''
        for i in range(len(digits)):
            k = str(digits[i])
            m = m + k
        l = int(m)+1
        r = list(map(int,str(l)))
        return r
      
